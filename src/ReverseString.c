#include "ReverseString.h"

int getStringLength(char *str)
{
  int i = 0 ;
  if (str == 0 || str == '\0')
  {
    return 0;
  }

  if (str == '\0')
  {
    return 0;
  }

/*  if (str != 0 && str != '\0' )
  {
    return 1;
  }
*/
}
